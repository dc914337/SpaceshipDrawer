namespace SpaceshipDrawer.Common
{
    struct Range2i
    {
        public int from;
        public int to;

        public Range2i(int from, int to)
        {
            this.from = from;
            this.to = to;
        }
    }
}
